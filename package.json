{
  "name": "carbon-ai",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Carbon AI — wrap a human operator as an Anthropic/OpenAI-compatible model API",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun --watch apps/gateway/src/index.ts",
    "start": "bun apps/gateway/src/index.ts",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  }
}
